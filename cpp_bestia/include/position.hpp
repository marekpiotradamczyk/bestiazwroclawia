#ifndef __POSITION__
#define __POSITION__

#include <vector>
#include <string>

namespace position {

    class Position {
        // variables here, like bitboard and so on
        public:
            Position(std::vector<std::string> tokens);
            

    };
}
#endif // __POSITION__